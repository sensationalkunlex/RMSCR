@model IEnumerable<InventoryRMSCR.Models.Fatory>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FactoryName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FactoryName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FatoryID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FatoryID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FatoryID })
        </td>
    </tr>
}

</table>
