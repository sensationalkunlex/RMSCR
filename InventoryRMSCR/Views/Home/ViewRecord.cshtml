
@model  List<InventoryRMSCR.Models.MasterFactorViewModel>

<link href="~/Content/PagedList.css" rel="stylesheet" type="text/css" />

@{
    ViewBag.Title = "Index";
}
@* Validate Date before Submit *@
<center>

    <h3 class="text-center">FEEDS: RAW MATERIALS STOCK CONTROL RECORD (RMSCR)  </h3>
    <h4 class="text-center">Select factory(UsedType) and input the Date(Date1 and Date2)</h4>
    <br />
    <div class="row">
        <div>
            @using (Html.BeginForm("getRecord", "Home", FormMethod.Get))
    {
        <center>
            <div class="row">
                <div class="col-md-3">

                    <span class="">Select </span>
                    @Html.DropDownList("Factory", ViewBag.Factory as SelectList, new { @class = "form-control" })
                    @* @Html.TextBox("SearchString", ViewBag.CurrentFilter as string, new { @class="form-control input-lg"})*@
                </div>
                <div class="col-md-3">
                    <div> Start Date</div>

                    <input type="text" name="startDate" id="datepicker" class="form-control" placeholder="  Start" />

                </div>

                <div class="col-md-3">
                    <div> End Date</div>
                    <input type="text" name="endDate" id="datepicker1" class="form-control" placeholder=" Date" />
                </div>

                <div class="col-md-3">
                    <span><br /></span>


                    <input type="submit" class="btn btn-info form-control" value="Search" />

                </div>
            </div>
        </center>

}
        </div>

    </div>
    <br />  <br />  <br /> <br /> <br /> <br /> <br />


    @Html.ActionLink("Get Product Transaction", "ViewProductTrans", null, new { @class = "btn btn-primary" })

    
    @Html.ActionLink("Get Previous Records   ", "Index","GetRecord", null, new { @class = "btn btn-primary" })
    <br />  <br />  <br />  <br /> <br /> <br /> <br /> <br />




    @*@if (Model != null)
            {
                <table class="table table-striped table-hover">
                    <tr>
                        <th>

                            Date
                        </th>
                        <th>
                            FactoryName
                        </th>
                        <th>
                            Total
                        </th>

                    </tr>
                    @foreach (var item in Model)
                    {
                        //Data acc Masterfirst

                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Masterrr.GetDate)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Masterrr.FactoryName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Masterrr.Total)
                            </td>

                        </tr>


                    }
                </table>
                <br />


                <table class="table table-striped table-hover">
                    <tr>
                        <th>

                            Date
                        </th>
                        <th>
                            ProductName
                        </th>
                        <th>
                            Value
                        </th>

                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Transaction.date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Transaction.FactoryName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Transaction.FactoryQtyU)
                            </td>

                        </tr>
                    }
                </table>
                <br />
            }
        </center>
    *@



</center>


@*@Styles.Render*@
<link href="~/Content/themes/base/jquery-ui.min.css" rel="stylesheet" />
@section Scripts{
<script src="~/Scripts/jquery-ui-1.12.1.min.js"></script>
<script>


    $(function () {
        $("#datepicker").datepicker({ dateFormat: "mm/dd/yy" });
    });

    $(function () {
        $("#datepicker1").datepicker({ dateFormat: "mm/dd/yy" });
    });
</script>
    }
@*Page @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) of @Model.PageCount
    @Html.PagedListPager(Model, page => Url.Action("Index",
        new { page, sortOrder = ViewBag.CurrentSort, currentFilter = ViewBag.CurrentFilter }))*@
